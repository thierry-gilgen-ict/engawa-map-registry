# Staging environment — placeholders only. Copy to .env on the staging host.
# Never commit a populated .env file.

NODE_ENV=production
DATABASE_URL=postgresql://POSTGRES_USER:POSTGRES_PASSWORD@postgres:5432/POSTGRES_DB
HOST=0.0.0.0
PORT=3000
TRUST_PROXY=true

POSTGRES_USER=engawa_registry
POSTGRES_PASSWORD=REPLACE_WITH_STRONG_PASSWORD
POSTGRES_DB=engawa_registry

ACME_EMAIL=REPLACE_WITH_OPERATOR_EMAIL

STAGING_HOSTNAME=staging-engawa-map.thierry-gilgen-ict.ch
